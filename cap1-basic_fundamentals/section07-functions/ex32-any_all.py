# All => Returns true if all elements are true
# Any => if any are true

# They both take a LIST as argument
print(all([True, True, True, True, False]))

statements = [True, False, False, False]
print(any(statements))